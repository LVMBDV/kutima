{
  "environments": {
    "test": {
      "addons":[
         "heroku-redis",
         "heroku-postgresql"
      ],
      "scripts": {
        "test-setup": "bundle exec rails db:setup"
      }
    }
  }
}
