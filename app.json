{
  "environments": {
    "test": {
      "addons":[
         "heroku-redis:in-dyno",
         "heroku-postgresql:in-dyno"
      ],
      "scripts": {
        "test-setup": "bundle exec rails db:setup",
        "test": "bundle exec rspec spec/"
      },
      "buildpacks": [
        { "url": "heroku/ruby" }
      ]
    }
  }
}
